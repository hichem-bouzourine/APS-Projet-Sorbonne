[
  PROC max [var result:int, x:int, y:int] [
    IF (lt x y) [SET result y] [SET result x]
  ];
  VAR res:int;
  VAR val1:int;
  VAR val2:int;
  SET val1 10;
  SET val2 20;
  CALL max (adr res, val1, val2);
  ECHO res
]
